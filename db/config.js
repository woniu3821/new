const config = {
    // 启动端口
    port: 3001,

    // 数据库配置
    database: {
        DATABASE: 'simon',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config
